[pytest]
testpaths = tests
# Coverage options (require pytest-cov to be installed)
# Uncomment these lines after running: pip install pytest-cov
# addopts =
#     --cov=fairy.cli
#     --cov=fairy.rulepack
#     --cov=fairy.validation.rulepack_runner
#     --cov-branch
#     --cov-report=term-missing
#     --cov-fail-under=35
filterwarnings =
    ignore::DeprecationWarning
